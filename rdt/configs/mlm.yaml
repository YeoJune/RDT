# MLM (Masked Language Model) Configuration with RoPE
# Standard BERT-style MLM using shared transformer backbone

# Model Type
model_type: "mlm"

# Model Architecture
model:
  # Architecture dimensions (defaults to BERT-base, override for custom size)
  d_model: 768 # Hidden dimension (BERT-base: 768)
  n_layers: 12 # Number of transformer layers (BERT-base: 12)
  n_heads: 12 # Number of attention heads (BERT-base: 12)
  d_ff: 3072 # Feed-forward dimension (default: 4 * d_model)

  # Sequence and regularization
  max_seq_len: 256 # Maximum sequence length
  dropout: 0.1 # Dropout probability

  # Special tokens
  mask_token_id: 103 # [MASK] token ID (BERT default: 103)
  pad_token_id: 0 # [PAD] token ID (BERT default: 0)

  # RoPE configuration
  rope_base: 10000.0 # RoPE frequency base

  # Weight tying
  tie_weights: true # Tie input/output embeddings (recommended)

# Training Strategy
training:
  # Batch and optimization
  batch_size: 64
  gradient_accumulation_steps: 1 # Effective batch = batch_size * gradient_accumulation_steps
  training_mode: "epoch" # "epoch" or "steps"
  num_epochs: 30
  max_training_steps: 100000

  # Learning rate and scheduling
  learning_rate: 0.0002 # 2e-4
  weight_decay: 0.01
  warmup_ratio: 0.1
  scheduler: "cosine"
  max_grad_norm: 1.0

  # Checkpointing
  save_every_n_epochs: 1
  save_every_n_steps: 1000
  keep_last_n_checkpoints: 3

  # Early stopping
  early_stopping:
    enabled: true
    patience: 5 # Number of evaluations without improvement before stopping
    min_delta: 0.0001 # Minimum change to qualify as improvement

  # BERT-style masking (80% [MASK], 10% random, 10% keep)
  # Set enabled: false to use 100% [MASK] for consistency with other baselines
  bert_masking:
    enabled: false

# Data
data:
  dataset_name: "wikitext-103"
  streaming: false
  max_seq_length: 256
  mlm_probability: 0.15 # Standard BERT masking ratio

  tokenizer_name: "bert-base-uncased"
  num_workers: 4
  pin_memory: true

# Logging & Checkpoints
output:
  checkpoint_dir: "checkpoints/mlm_baseline"
  log_dir: "runs/mlm_baseline"
  log_every_n_steps: 100
  eval_every_n_epochs: 1
  eval_every_n_steps: 1000
  use_tqdm: true

# Hardware
device: "cuda"
mixed_precision: false
seed: 42
